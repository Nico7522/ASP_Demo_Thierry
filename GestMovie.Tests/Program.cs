using GestMovie.Mvc.Models.Data.Entities;
using GestMovie.Mvc.Models.Data.Repositories;
using GestMovie.Mvc.Models.Data.Services;

namespace GestMovie.Tests
{
    internal class Program
    {
        static void Main(string[] args)
        {

            //IMovieRepository repository = new MovieService();
            ////Movie movie = new Movie() { Nom = "P'tite Annick", Annee = 1997, Realisateur = "James Cameron" };

            ////movie = repository.Insert(movie);

            ////Console.WriteLine(movie.Id);

            //IEnumerable<Movie> movies = repository.Get();
            //foreach(Movie movie in movies)
            //{
            //    Console.WriteLine($"{movie.Id} {movie.Nom}");
            //}
        }
    }
}
